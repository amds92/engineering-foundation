# [Project Name]

[One sentence: what it does and for whom.]

## Stack
- **Language:** [e.g. Ruby 3.3]
- **Framework:** [e.g. Rails 8.1 API-only]
- **Database:** [e.g. PostgreSQL]
- **Jobs:** [e.g. Sidekiq / SolidQueue / none]
- **Auth:** [e.g. JWT / Devise / none]
- **Tests:** [e.g. RSpec]
- **Lint:** [e.g. RuboCop]
- **Deploy:** [e.g. Kamal / Fly.io / Heroku]

## Commands
```sh
# Install
[e.g. bundle install]

# Test
[e.g. bundle exec rspec]

# Single test
[e.g. bundle exec rspec spec/services/foo_spec.rb]

# Lint
[e.g. bundle exec rubocop]

# Start
[e.g. bin/dev]
```

## Structure
[Only what's non-obvious. Skip standard conventions.]
- `app/services/` — all business logic
- `app/clients/` — all external API calls, nowhere else
- `app/queries/` — complex ActiveRecord queries

## Git flow
- Branches: `feat/`, `fix/`, `chore/`, `refactor/`
- Commits: Conventional Commits (`feat:`, `fix:`, `chore:`)
- Never push to main — PRs only

## Rules
- [Non-obvious rule — e.g. "All monetary values in cents, never floats"]
- [Non-obvious rule — e.g. "Never hard-delete records, use soft deletes"]
- [Non-obvious rule — e.g. "Background jobs must be idempotent"]

## IMPORTANT
- [Critical constraint — e.g. "DO NOT touch LegacyPaymentService — rewrite in progress"]
- [Critical constraint — e.g. "Check app/services/ before creating anything new"]

## Things that will bite you
- [e.g. "`User#full_name` returns nil for OAuth users — use `User#display_name`"]
- [e.g. "Test suite requires Redis running — `redis-server` before `rspec`"]
- [e.g. "`Order#total` is memoized — call `reload` after payment updates"]
